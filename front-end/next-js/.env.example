# Browser-safe public build-time configuration only.
# Do not put OAuth settings, tokens, database credentials, Fernet keys,
# session secrets, or backend credentials in this file.
NEXT_PUBLIC_BACKEND_URL=http://localhost:8000